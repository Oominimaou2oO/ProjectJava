package ascenseur.traitement.requete;

public class RequeteInterne extends Requete {

	/**
	 * Constructeur, qui appelera le constructeur de super
	 * @param etage L'étage à atteindre.
	 * @see Requete
	 */
	public RequeteInterne(int etage) {
		super(etage);
	} // Constructeur
	
}
